export interface Type {
  aperitif: boolean;
  entree: boolean;
  plat: boolean;
  dessert: boolean;
}

export interface PrepTime {
  short: boolean;
  medium: boolean;
  long: boolean;
}

export interface Difficulty {
  easy: boolean;
  medium: boolean;
  hard: boolean;
}
